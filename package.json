
{
	"name":    "engram",
	"version": "0.5.0",

	"scripts": {
		"start":    "./bin/docopt-engram.js --port=5000",
		"rebuild":  "bash build-docs.sh && bash build-es6.sh",
		"test":     "mocha node_modules/engram/test --timeout=20000"
	},

	"bin": {
		"engram": "node ./bin/docopt-engram.js"
	},

	"dependencies": {
		"is":           "rgrannell1/is",
		"express":      ">=4.10.2",
		"sqlite3":      ">=3.0.5",
		"content-type": "*",
		"mustache":     "*",
		"docopt":       "*",
		"valid-url":    "*",
		"supertest":    "*"
	}
}
