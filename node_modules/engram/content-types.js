#!/usr/bin/env node
"use strict";

var isHTML = function (contentType) {
	return contentType.type === "text/html";
};

var isPDF = function (contentType) {
	return contentType.type === "application/pdf";
};

module.exports = {
	isHTML: isHTML,
	isPDF: isPDF
};
