
"use strict"




var redirectToHTTPS = require('engram/rest/redirect-to-https')





var handlers = {
	http:  {
		get:    { },
		post:   { },
		delete: { },
		all:    { }
	},
	https: {
		get:    { },
		post:   { },
		delete: { },
		all:    { }
	}
}





handlers.https.get['/bookmarks'] = [
	(req, res) => {
		res.send( '' )
	}
]

handlers.https.post['/import']  = [
	(req, res) => {
		res.send( '' )
	}
]

handlers.https.get['/bookmarks/:id']  = [
	(req, res) => {
		res.send( '' )
	}
]

handlers.https.delete['/bookmarks/:id']  = [
	(req, res) => {
		res.send( '' )
	}
]

handlers.https.get['/archive/:id']  = [
	(req, res) => {
		res.send( '' )
	}
]





module.exports = handlers
