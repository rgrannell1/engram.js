
"use strict"




var express   = require('express')
var constants = require('engram/commons/constants')
var specs     = require('engram/rest/route-specs')
var augment   = require('engram/rest/augment')





var impls = [
	( ) => {

		var route = {
			id:     constants.routes.ids.GET_PUBLIC_FILES,

			method: 'use',
			main:    [express.static('public')]
		}

		return route

	}
]





var addRoutes = specs => {

	return impls.reduce((specs, patch) => {
		return augment(specs, patch( ))
	}, specs)

}





module.exports = addRoutes
