#!/usr/bin/env node
"use strict";

var is = require("is");
var log = require("engram/logger");

var Result = require("engram/result").Result;
var Ok = require("engram/result").Ok;
var Err = require("engram/result").Err;
var utils = require("engram/utils");
var STATSBUS = require("engram/stats");

var startJobs = function (jobs) {

	var pids = Object.keys(jobs).map(function (jobname) {
		return jobs[jobname];
	}).map(function (_ref) {
		var job = _ref.job;
		var reporter = _ref.reporter;
		var interval = _ref.interval;

		return setInterval(job.bind({}, reporter), interval);
	});
};

module.exports = startJobs;