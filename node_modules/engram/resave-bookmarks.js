#!/usr/bin/env node
"use strict";

var log = require("engram/logger");

var Result = require("engram/result").Result;
var Ok = require("engram/result").Ok;
var Err = require("engram/result").Err;

var saveBookmark = require("engram/save-bookmark");
var createArchive = require("engram/create-archive");
var log = require("engram/logger");
var database = require("engram/database");

var request = require("request");

var resaveBookmark = function (bookmark) {};

var resaveBookmarks = function (url, time, _ref) {
	var req = _ref.req;
	var res = _ref.res;

	imports.forEach(resaveBookmark);
};

module.exports = resaveBookmark;
