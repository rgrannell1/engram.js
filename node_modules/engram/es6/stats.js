#!/usr/bin/env node

"use strict"





var is        = require('is')
var log       = require('engram/logger')

var Result    = require('engram/result').Result
var Ok        = require('engram/result').Ok
var Err       = require('engram/result').Err

var constants = require('engram/constants')





constants.BUS.on(':title', saveResult => {

	saveResult.onErr(err => {
		log.error({message: err.message, stack: err.stack}, 'title extraction failed.')
	})

	saveResult.onOk(url => {

		if (!is.undefined(url)) {
			log.info(`successfully extracted title for ${url}`)
		}

	})

})

constants.BUS.on(':softfail', saveResult => {

	saveResult.onErr(err => {
		log.error({message: err.message, stack: err.stack}, 'soft-fail determination failed.')
	})

	saveResult.onOk(url => {

		if (!is.undefined(url)) {
			log.info(`successfully determinated soft-fail status for ${url}`)
		}

	})

})

constants.BUS.on(':status', saveResult => {

	saveResult.onErr(err => {
		log.error({message: err.message, stack: err.stack}, 'status determination failed.')
	})

	saveResult.onOk(url => {

		if (!is.undefined(url)) {
			log.info(`successfully determinated fail status for ${url}`)
		}

	})

})

constants.BUS.on(':archive', archiveResult => {

	archiveResult.onErr(err => {
		log.error({message: err.message, stack: err.stack}, 'status determination failed.')
	})

	archiveResult.onOk(url => {

		is (!is.undefined(url)) {
			log.info(`successfully saved archive for ${url}`)
		}

	})

})
