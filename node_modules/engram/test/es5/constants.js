#!/usr/bin/env node
'use strict';

var constants = {
	PORT: 5000,
	KEYPATH: 'certs/key.pem',
	CERTPATH: 'certs/cert.pem'
};

constants.HOST = 'https://localhost:' + (constants.PORT + 1);

constants.DEFAULT_CONFIG = {
	port: constants.PORT,
	dbpath: ':memory',
	keypath: constants.KEYPATH,
	certpath: constants.CERTPATH,
	start: true
};

module.exports = constants;
