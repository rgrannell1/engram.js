#!/usr/bin/env node
"use strict";

var should = require("should");
var engram = require("engram/engram");

describe("/*", function () {

	it("should 404 for non-http URLs", function () {

		engram(5000);
	});
});