
"use strict"




var constants = require('./constants')
var commons   = require('./commons')
var server    = require('../app/start-server')
var mocha     = require('mocha')









describe('', ( ) => {

	var testApps

	before(done => {

		server(commons.defaultArgs({ }), apps => {
			testApps = apps
			done( )
		})

	})

	describe('server', ( ) => {
		it('', done => {

			done( )

		})
	})

})
