
"use strict"




var constants = require('./constants')
var commons   = require('./commons')
var server    = require('../app/start-server')
var mocha     = require('mocha')

var expect    = require('chai').expect




describe('- routes', function ( ) {

	var testApps
	var testServers
	this.timeout(10000)

	before(done => {

		server(commons.defaultArgs({ }), (apps, servers) => {

			testApps    = apps
			testServers = servers

			done( )
		})

	})

	it('has an explicit spec object describing rosutes', ( ) => {

		expect(testApps.http.specs).to.be.an('array')
		expect(testApps.https.specs).to.be.an('array')

	})

	after(done => {

		testServers.http.close(( ) => {
			testServers.https.close(done)
		})

	})

})
