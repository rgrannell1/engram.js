
"use strict"




var assert    = require('assert')

var constants = require('./constants')
var commons   = require('./commons')
var server    = require('../app/start-server')
var mocha     = require('mocha')





describe('- routes', function ( ) {

	var testApps
	var testServers
	this.timeout(10000)

	before(done => {

		server(commons.defaultArgs({ }), (apps, servers) => {

			testApps    = apps
			testServers = servers

			done( )
		})

	})

	xit('has an explicit spec object describing routes', done => {

	})

	after(done => {

		process.nextTick(( ) => {

			testServers.http.close(( ) => {
				testServers.https.close(done)
			})

		}, 0)

	})

})
