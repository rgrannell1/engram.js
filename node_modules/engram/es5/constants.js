'use strict';

var events = require('events');

var constants = {};

constants.SECOND_IN_MS = 1000;

constants.BUS = new events.EventEmitter();

constants.CACHE_SIZE = 100;
constants.REQUEST_COUNT = 5;
constants.REQUEST_WAIT = 0.5 * constants.SECOND_IN_MS;
constants.HTTP_TIMEOUT = 1.0 * constants.SECOND_IN_MS;

constants.REAUTH_TIMEOUT = 1.0 * constants.SECOND_IN_MS;

constants.REQUEST_ID_SIZE = 20;

constants.CERT_EXPIRATION = 356;
constants.CERT_KEYSIZE = 4096;
constants.CERT_COUNTRY = 'IE';
constants.CERT_COMMONNAME = 'Engram Self-Signed';

constants.USER_AGENT = 'Mozilla/5.0 (X11; Linux x86_64) AppleWebKit/537.36 (KHTML, like Gecko) Ubuntu Chromium/41.0.2272.76 Chrome/41.0.2272.76 Safari/537.36';

constants.CREDENTIAL_PATH = 'certs/login.txt';

module.exports = constants;
