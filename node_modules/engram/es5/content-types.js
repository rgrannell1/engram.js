#!/usr/bin/env node


"use strict";

var isHTML = function isHTML(contentType) {
	return contentType.type === 'text/html';
};

var isPDF = function isPDF(contentType) {
	return contentType.type === 'application/pdf';
};

module.exports = {
	isHTML: isHTML,
	isPDF: isPDF
};
